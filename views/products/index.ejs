<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=<<h1>>, initial-scale=1.0" />
    <title>Beaver Wood Carvers Products</title>
    <link rel="stylesheet" href="/app.css" />
  </head>

  <body>
    <h1>Beaver Wood Carvers Products - index page</h1>
    <nav>
      <a href="/products/new">Add a New Product</a>
    </nav>
    <ul>
      <% products.forEach((product, index) => { %>

      <li>
        <a href="/products/<%=product.id%>"><%=product.productName%> </a>
        <br> </br>
            <%=product.productDescription%>
        
        <br> </br>
        <%=product.productCategory%>
      
      <img src="<%=product.img%>" alt="" />
    </li>
            <%if(product.Customer.id ===1) { %>}
      <form action="/products/<%=product.id%>?_method=DELETE" method="POST">
        <input type="submit" value="Delete" />
      </form>
      <!-- Add edit link here  -->
      <a class="edit" href="/products/<%=product.id; %>/edit">Edit</a>
    <form action="/products/<%=product.id%>?_method=DELETE" method="POST">
        <input type="submit" value="Delete" />
    </form>
        <% } %>
      <% }) %>
    </ul>
  </body>
</html>
